import GlobalStyle from "../../Assets/GlobalStyles/GlobalStyle";
import {SafeAreaView} from "react-native";

const ProfileScreen = () => {
    return (
        <SafeAreaView style={[GlobalStyle.globalBackgroundFlex, {backgroundColor: '#00FF00'}]}>

        </SafeAreaView>
    );
};

export default ProfileScreen;
